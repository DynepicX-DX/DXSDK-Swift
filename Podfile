platform :ios, '9.0'

target 'PPSDK-Swift' do
  use_frameworks!
  
  pod 'Alamofire', '~> 4.7'
  pod 'KeychainSwift', '~> 12.0'

  target 'PPSDK-SwiftTests' do
      pod 'Quick'
      pod 'Nimble'
  end

end
